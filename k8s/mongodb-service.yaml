apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: namegen
spec:
  clusterIP: None

  selector:
    app: mongodb

  ports:
    - name: mongodb
      protocol: TCP
      port: 27017
      targetPort: 27017
