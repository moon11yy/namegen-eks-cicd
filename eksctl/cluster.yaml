apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: namegen-eks
  region: eu-central-1

autoModeConfig:
  enabled: true
  nodePools:
    - general-purpose
    - system

accessConfig:
  authenticationMode: API_AND_CONFIG_MAP

iam:
  withOIDC: true
